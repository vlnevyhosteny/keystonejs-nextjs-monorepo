{
  "private": true,
  "name": "keystonejs-nextjs-monorepo",
  "scripts": {
    "format": "prettier --config .prettierrc.js './**/*.ts' --write",
    "prepare": "husky install"
  },
  "devDependencies": {
    "chalk": "^2.0.1",
    "husky": "^7.0.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
